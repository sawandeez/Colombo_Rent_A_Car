{
  // Keep Java/Maven project model in sync with pom.xml automatically
  "java.configuration.updateBuildConfiguration": "automatic",

  // Ensure Maven import is enabled for the Java language server
  "java.import.maven.enabled": true
}
